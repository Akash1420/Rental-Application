/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FilterOccupancyComponent } from './filter-occupancy.component';

describe('FilterOccupancyComponent', () => {
  let component: FilterOccupancyComponent;
  let fixture: ComponentFixture<FilterOccupancyComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FilterOccupancyComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FilterOccupancyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
