/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FilterFurnishingComponent } from './filter-furnishing.component';

describe('FilterFurnishingComponent', () => {
  let component: FilterFurnishingComponent;
  let fixture: ComponentFixture<FilterFurnishingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FilterFurnishingComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FilterFurnishingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
