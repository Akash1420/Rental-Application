/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LocationFilteredComponent } from './location-filtered.component';

describe('LocationFilteredComponent', () => {
  let component: LocationFilteredComponent;
  let fixture: ComponentFixture<LocationFilteredComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LocationFilteredComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LocationFilteredComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });
 
  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/     
