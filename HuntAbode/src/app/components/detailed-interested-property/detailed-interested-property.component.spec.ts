/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DetailedInterestedPropertyComponent } from './detailed-interested-property.component';

describe('DetailedInterestedPropertyComponent', () => {
  let component: DetailedInterestedPropertyComponent;
  let fixture: ComponentFixture<DetailedInterestedPropertyComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DetailedInterestedPropertyComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DetailedInterestedPropertyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
