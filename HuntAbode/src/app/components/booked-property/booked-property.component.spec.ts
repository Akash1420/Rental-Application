/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BookedPropertyComponent } from './booked-property.component';

describe('BookedPropertyComponent', () => {
  let component: BookedPropertyComponent;
  let fixture: ComponentFixture<BookedPropertyComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BookedPropertyComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BookedPropertyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
