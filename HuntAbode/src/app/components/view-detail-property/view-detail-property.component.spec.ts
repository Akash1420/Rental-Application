/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ViewDetailPropertyComponent } from './view-detail-property.component';

describe('ViewDetailPropertyComponent', () => {
  let component: ViewDetailPropertyComponent;
  let fixture: ComponentFixture<ViewDetailPropertyComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ViewDetailPropertyComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ViewDetailPropertyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
